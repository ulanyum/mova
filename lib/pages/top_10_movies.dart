import 'package:flutter/material.dart';

class Top10Film extends StatelessWidget {
  const Top10Film({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
appBar: AppBar(),

body: Stack(
  children: [
        Container(
      height: double.infinity,
      color: Color.fromARGB(255, 25, 26, 32),
    
    ),
  ],
),
    );
  }
}